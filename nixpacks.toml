[phases.setup]
nixPkgs = ["rustup", "openssl", "pkg-config", "cacert"]

[phases.install]
cmds = ["rustup default nightly", "cargo fetch"]

[phases.build]
cmds = ["cargo build --release"]

[start]
cmd = "./target/release/safety-net-backend"
